# Core
numpy>=1.24
pyyaml>=6.0
tqdm>=4.66

# Video / image IO
opencv-python>=4.8

# Live screen capture (real-time recognizer: src.infer.live)
mss>=9.0

# Deep learning (install the torch build matching your platform)
#   Mac (Apple Silicon, MPS):  pip install torch torchvision
#   CUDA cloud GPU:            pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16

# x3d_s backbone (torch.hub loads facebookresearch/pytorchvideo)
pytorchvideo>=0.1.5
fvcore>=0.1.5
iopath>=0.1.10

# Metrics / reporting
scikit-learn>=1.3
